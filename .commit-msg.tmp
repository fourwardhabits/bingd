Point the data model at the migrations as source of truth

Records the three places the migrations deliberately depart from the SQL
sketched in the document, so a reader who compares them is not left guessing
which one is authoritative.