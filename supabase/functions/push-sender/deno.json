{
  "//": "Same reasoning as tmdb-adapter/deno.json: Bingd's package.json sits three directories up, and without this Deno treats it as the dependency manifest for this function — then fails to resolve the npm: specifier below, because supabase-js is installed for React Native rather than for Deno. 'none' tells it to fetch npm dependencies itself, which is also what the deployed function does.",
  "nodeModulesDir": "none",
  "imports": {
    "@supabase/supabase-js": "npm:@supabase/supabase-js@2.58.0",
    "@std/assert": "jsr:@std/assert@1"
  }
}
